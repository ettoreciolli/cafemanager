{
  "name": "cafemanager",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "scripts": {
    "dev": "turbo dev --filter=!@cafemanager/db",
    "dev:web": "turbo dev --filter=web",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean",
    "sim": "bun ./packages/ordersim/src/index.ts",
    "seed": "bun --cwd packages/db run db:seed",
    "db:validate": "cd packages/db && bun run db:validate",
    "db:generate": "cd packages/db && bun run db:generate",
    "db:push": "cd packages/db && bun run db:push"
  }
}

